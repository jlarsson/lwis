(function (module) {
    'use strict';

    var fs = require('fs');
    var path = require('path');
    var parser = require('./transform-parser');

    /*
    var parsed = parser.parse('when {id} == :id && 1 > 2', {
        require: require
    });
    console.log('%j', parsed);
*/
    module.exports = function () {};
})(module);