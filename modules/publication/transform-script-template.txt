(function (){
  'use strict';
  if (arguments[0].command == 'getFilter') {
    try{
      return filter instanceof Function ? arguments[0].secret : null;
    }
    catch (e) {
      if (e instanceof ReferenceError) {
        return null;
      }
      throw e;
    }
  }
  if (arguments[0].command == 'getTransform') {
    try{
      return transform instanceof Function ? arguments[0].secret : null;
    }
    catch (e) {
      if (e instanceof ReferenceError) {
        return null;
      }
      throw e;
    }
  }
  $param-initialization$;
  $user-code$;
  if (arguments[0].command == 'applyFilter') {
    return arguments[0].filter.self.filter( function (item) { return filter.call(item, this.args); },arguments[0].filter);
  }
  if (arguments[0].command == 'applyTransform') {
    return transform.apply(arguments[0].transform.self, arguments[0].transform.args);
  }
})($global$);
